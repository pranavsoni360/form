# Core Agent Framework (match Vaani production versions)
livekit-agents==1.3.11
livekit==1.1.2
livekit-api==1.1.0

# Plugins
livekit-plugins-deepgram==1.3.11
livekit-plugins-google==1.3.11
livekit-plugins-silero==1.3.11
livekit-plugins-sarvam==1.3.11
livekit-plugins-openai==1.3.11
livekit-plugins-groq==1.3.11
# Semantic end-of-utterance model (multilingual incl. Hindi). Model files come
# from HuggingFace: run `python los_updated.py download-files` once per box.
livekit-plugins-turn-detector==1.3.11

# Utilities
python-dotenv~=1.0
aiohttp>=3.11.11
pytz
requests

# Error tracking → self-hosted GlitchTip (Sentry-compatible). No-op unless SENTRY_DSN_AGENT set.
sentry-sdk>=2.0
