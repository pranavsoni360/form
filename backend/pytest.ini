[pytest]
# asyncio_mode=auto so `async def` tests run without a per-test marker.
# Only affects coroutine tests; sync tests are unaffected.
asyncio_mode = auto
testpaths = tests agent/tests services/tests lrs/tests
