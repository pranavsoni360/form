# Web framework
fastapi
uvicorn[standard]
python-multipart
aiofiles

# Database
asyncpg

# Security & Authentication
bcrypt
PyJWT
python-jose[cryptography]

# HTTP client
httpx

# Environment variables
python-dotenv

# Data validation
pydantic
email-validator

# PDF generation
fpdf2

# Scheduling (agent batch runner)
apscheduler

# Date/Time
python-dateutil
pytz

# Utilities
phonenumbers

# Audit geolocation — offline IP->geo reader (GeoLite2 / DB-IP .mmdb).
# Pure reader, no network; geo is a no-op if GEOIP_DB_PATH is unset.
maxminddb

# Agent module (voice calling, batch processing, post-call analysis)
# Without these the /api/agent/* routes are disabled at startup
aiohttp
pandas
livekit-api
livekit-agents
google-genai

# Observability (M1): structured JSON logs + error tracking
# sentry-sdk auto-captures unhandled FastAPI exceptions, scrubbed for PII
# python-json-logger formats every log line as ECS-style JSON
sentry-sdk[fastapi]
python-json-logger
